# Example testing env (Postgres). Use these when running integration tests against the Postgres service.
DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=greenlit
DB_USERNAME=greenlit
DB_PASSWORD=secret

# If running tests inside the app container via docker compose, these
# values will allow the container to reach the Postgres service named `db`.
# Use the composer script `composer test:docker:pgsql` to run tests against Postgres.
